apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Default namespace for same-origin deployment resources.
namespace: rocketchat
resources:
  # Keep nginx config and workload split so operators can override only what they need.
  - configmap-nginx.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
